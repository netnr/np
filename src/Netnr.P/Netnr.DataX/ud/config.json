{
    //表映射完整匹配（默认忽略符号、大小写）
    "mapping-table-full-match": false,
    //列映射完整匹配（默认忽略符号、大小写）
    "mapping-column-full-match": false,
    //连接信息
    "db-conn": [{
        "type": "SQLite",
        "conn": "Data Source=D:\\tmp\\tmp.db",
        "remark": "Local tmp"
    }]
}